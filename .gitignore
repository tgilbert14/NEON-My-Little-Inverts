.Rproj.user
.Rhistory
.RData
.Ruserdata
.neon_token
.vs/
.vscode/
node_modules/
*.Rproj.bak

# the raw NEON fetch lives one dir up and is NEVER committed (the app is
# bundle-only — it reads data/sites/*.rds, never the raw stack)
../inverts-data-fetch/

# review-agent + boot-test + QA scratch (these never ship; match at any depth)
_*.R
_*.png
_*.pdf
_adv_check*.R
_verify_*.R
_boot.R
_inspect.R
